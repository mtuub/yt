{
    "scripts": {
        "build": "tsc -p .",
        "scrape-horoscope": "npm run build && node build/server/scrape_horoscopes.js"
    },
    "devDependencies": {
        "ts-node": "^10.8.1",
        "typescript": "^4.6.2"
    },
    "dependencies": {
        "axios": "^1.2.3",
        "dotenv": "^16.0.3"
    }
}